{{input type="checkbox"
				id=idValue
				class="form-check-input"
				name=name
				checked=value
				change=(action 'ifYesToggle')
				data-toggle=toggle}}
<label class="form-check-label checkbox" for={{idValue}}>
	{{label}}
</label>
